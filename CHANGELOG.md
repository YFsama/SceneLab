# Changelog

All notable changes to SceneLab are documented here. Dates are YYYY-MM-DD.

## [0.18.0] — 2026-09-22

参数表 + 沉头孔 + 启动性能轮（多 agent）。

### 参数化 (Parametric)

- **参数表面板 (Parameters)**（Fusion Parameters 对话框对标）：命令面板
  "Parameters…" 打开全文档驱动参数表——特征参数（拉伸距离/旋转角/
  圆角半径/倒角/抽壳/比例目标/孔径孔深/阵列数与间距/扫掠扭转）与当前
  草图的驱动约束（距离/半径）逐行列出、就地编辑（Enter 提交/Esc 取
  消/非法值保持原值），修改直达特征树与草图求解器，抑制特征置灰；
  表格随 featureVersion 实时刷新。
- **沉头孔/锥沉孔 (Counterbore/Countersink)**：孔特征新增可选沉头
  （第二段宽圆柱）与锥沉（截锥，含角精确、深度钳制后重算截断半径保
  角度不变）；时间轴摘要用 GD&T 符号（⌀6×∞ ⌴10×3 / ⌵10°90）；
  FeatureEditor 全字段表达式编辑 + 交叉校验（沉头必须比孔宽等）。
  体积数学实测比值 0.994（32 边形切割器的恒定系数）。

### 性能 (Performance)

- **Vendor 拆分**：主 chunk 1441 KB → 640 KB（gzip 385 → 168 KB）；
  three.js / React / 图标独立 chunk，各自缓存、并行加载。
- **WASM 预热空闲化**：Manifold 布尔引擎的 0.5 MB 加载推迟到
  requestIdleCallback，不再与首屏渲染抢带宽。

### 修复 (Fixes)

- **撤销/重绘第三处同根缺陷**：applyUndoSnapshot 现在递增
  featureVersion——此前 Ctrl+Z 后时间轴与特征列表不重绘（E2E 轮次
  发现，前两处为键盘重排与菜单施加修改特征路径）。

## [0.17.0] — 2026-09-22

孔特征轮 + QA 覆盖清扫（多 agent）。

### 建模 (Modeling)

- **参数化孔特征 (Hole)**（Fusion 最常用特征，此前缺失）：实体右键 →
  Feature → Hole，两步输入直径与深度（**0 = 通孔**）——默认从顶面质心
  沿 −Y 钻入；时间轴获得可编辑的孔节点（⌀8×∞ 摘要），直接体走可撤
  销直接编辑；求差用精确 Manifold 布尔（体素回退），贯穿孔以包围盒对
  角线×2 保证完全穿透。`.studio3d` 往返、抑制、消费父特征语义与其它修
  改特征一致。AI 工具 `create_hole`（直径+深度/通孔，可选 x/y/z 或默
  认顶面）。
- 附带修复：修改特征公共路径漏 `featureVersion` 递增（圆角/倒角/抽壳/
  阵列经菜单施加时时间轴芯片同样会滞留——与上轮键盘重排同根因）。

### 测试 (QA)

- **E2E 12 → 15 条**：草图偏移全流程（含撤销）、自动恢复横幅（手工构
  造存档 JSON 种子 → 横幅出现/丢弃清键/恢复回体）。
- **组件测试 +10**：CommandPalette（过滤/键盘导航/执行/Esc）、
  SectionPanel（轴切换/偏移滑杆/翻转/关闭）——两者此前零覆盖。
- 行为澄清入库：圆偏移 = 实体计数 +2（圆环 + 新圆心点，addCircle 的
  既有约定），E2E 断言按真实行为书写。

## [0.16.0] — 2026-09-21

数据安全 + 脏标记保真 + 四缺陷修复轮（多 agent）。

### 数据安全 (Data safety)

- **启动时自动恢复横幅**（Fusion/Office 式）：崩溃/误关后再次启动，底部
  横幅提示"发现 '<项目名>' 的未保存自动存档（N 分钟前）"，一键 **恢复**
  （走既有反序列化路径，恢复后保持脏标记）/ **丢弃**（清除 localStorage
  存档）/ 关闭（本会话不再提示，存档保留）。调查确认此前启动时从不主动
  恢复——横幅为纯增量，无双重恢复风险。中英双语、键盘可达（Esc 关闭）。

### 保真修复 (Fidelity fixes)

- **脏标记保真**：显式保存/加载/新建时捕获状态指纹；撤销/重做后与指纹
  对比——**回到恰好已保存的状态时脏点自动熄灭**（Office 行为），此前一
  旦编辑过就永远显示未保存。
- **时间轴键盘重排渲染修复**（E2E 发现）：特征树原地变更导致订阅者收
  不到通知，Alt+←/→ 重排后芯片标签滞留旧序号；新增 `featureVersion`
  计数器，所有树变更都会触发 TimelineBar/FeatureEditor 重渲染。
- **状态栏溢出修复**：选中提示文案在 1280 宽度下换行溢出、遮挡时间轴
  芯片点击；改为截断不换行。
- **测量 HUD 遮挡修复**：HUD 从顶部下移避开浮动工具条；模式按钮不再被
  拦截。
- **拉伸对话框 Enter 提交**：输入框内按 Enter 直接提交（原先依赖按钮
  自动聚焦，不可靠）。

### 测试 (QA)

- **E2E 9 → 12 条**：时间轴 HTML5 拖拽重排 + Alt+→ 键盘重排（真实
  sketch→extrude×2 流程构建）、测量三点角度 + 面积模式读数（20mm 立方
  面 = 400 mm² 精确断言）。Playwright 支持 `E2E_PORT` 环境变量（宿主
  端口被 Docker 代理占用时可换端口）。
- **AI**：`measure_face_area` 工具（单面面积+质心，或全部面列表+总面积）。

## [0.15.0] — 2026-09-21

测量工具补全 + 草图入口修复 + E2E 扩容（多 agent 轮）。

### 测量 (Measure)

- **角度与面积测量**（Fusion Measure 对齐，此前仅距离）：HUD 顶部新增
  距离/角度/面积三模式切换——角度取三点（顶点居中，∠ 于中间点），
  面积点击面即得（扇形三角化面积 + 面积加权质心，Newell 法校验）；
  角度结果可存为持久标注；模式切换自动清空拾取。纯数学含旋转不变
  性等 22 个新测试。

### 修复 (Fixes)

- **进入草图工作区即真正开始草图**：此前工具栏/S 键只置 sketchActive
  而不建 currentSketch，所有绘图工具静默失效，必须先点基准面。现在
  无草图时按当前/默认平面自动创建，工具立即可用（E2E 发现的真实缺
  陷）；已有草图则原样恢复。

### 测试 (QA)

- **E2E 6 → 9 条**：草图修剪全流程（实体计数 6→5→撤销还原 6）、拉
  伸对话框表达式输入（`20/2` 实时 `= 10` 预览 → 时间轴出现 Extrude
  芯片）、工程图注释流程（放置/编辑/脏标记/撤销深度）。新用例各
  3 连跑验证稳定。

## [0.14.0] — 2026-09-21

修剪补全 + AI 草图编辑 + 对话框双语清零。

### 草图 (Sketch)

- **圆弧修剪**：修剪现在支持圆弧——沿弧自身扫掠方向参数化，交点仅在
  弧段范围内的才作为切割点；点击段删除、其余段保留为同圆心/半径的
  圆弧（构造标志随行）；无交叉的弧整体删除。CW（负扫掠）弧同样支持。
- **圆/弧多点切割**：4 交点的圆删除一段后保留 **3 段圆弧**（此前强并
  为单段互补弧）——与 Fusion 一致，不合并不连通的段。

### AI

- **`trim_sketch_entity` / `extend_sketch_entity` / `offset_sketch_entity`**
  三个草图编辑工具：AI 现在能在对话中修剪/延伸/偏移自己的草图（用
  draw_* 返回的实体 id + 草图坐标），失败给出可重试的明确错误。

### 本地化 (i18n)

- **FeatureEditor 全部编辑对话框双语化**（拉伸/旋转/数值特征编辑 +
  无参数回退）：此前是应用里最后一批硬编码英文界面；8 个新键对补齐
  en/zh，中文采用既有 CAD 术语（编辑拉伸/半径 (mm)/保留原实体…）。

## [0.13.0] — 2026-09-21

草图 2D 工具集补全轮 + 测量性能。

### 草图 (Sketch)

- **修剪 Trim**（Fusion/SolidWorks 经典工具，此前缺失）：工具栏剪刀图标
  进入修剪模式后，点击任意线段/圆的某一段即删该段——被交叉分割的线
  保留其余段（保留段按位置复用既有接合点，约束不丢）；点击无交叉的
  图元则整条删除（Fusion "trim to nothing"）；圆被线切开后点击一段，
  剩余部分转为圆弧。工具保持激活可连续修剪，每次一条草图撤销记录。
- **延伸 Extend**：点击方向延伸选定图元到最近的交叉边界——线移动靠
  近点击端的端点、圆弧沿自身扫掠方向扩到与线的交点；无边界则提示并
  保持不变。
- 未命中给出中英双语提示（可修剪对象/无延伸边界）。

### 性能 (Performance)

- **测量模式悬停零重复分配**：吸附目标（顶点 + 边中点 + 面心）按
  body 引用缓存，大导入体的测量悬停不再每帧重建数千个点。

### 测试 (QA)

- 新 E2E：M 键切换测量（aria-pressed 断言）+ 状态栏撤销深度指示。

## [0.12.0] — 2026-09-20

工程图状态持久化轮：图纸成为文档的一部分。

### 持久化 (Persistence)

- **工程图随 `.studio3d` 保存/加载**：剖切轴、全部详图定义与文字注释进
  入项目文件的 `drawing` 块（可选字段——旧文件无此块时安全回退为默
  认值，文件版本不变）。保存（含 Ctrl+S、原生保存、自动保存/崩溃恢
  复快照）与打开（浏览器/原生/自动恢复）全部走同一路径；工程图编辑
  现在会正确置脏文档。
- **工程图进入撤销历史**：撤销快照扩展为「场景 + 特征树 + 图纸」三合一
  ——加/改/删注释、加/删详图、切换剖切轴各占一条撤销记录，Ctrl+Z 在
  建模与图纸编辑间无缝往返。

### AI

- **`add_drawing_note` 工具**：AI 可直接在图纸上添加技术要求/表面处
  理说明等注释；省略坐标时沿左边距自动堆叠（连续调用永不重叠），
  编辑同样入撤销历史。

## [0.11.0] — 2026-09-20

todo 清零轮：键位重排 + 工程图详图/注释 + AI 拾取确认。

### 操作 (Interaction)

- **M = 测量（SolidWorks/Onshape 肌肉记忆）**：普通 M 切换测量工具；
  模型工作区跳转移至 **Shift+M**（Esc 本就能从草图返回模型）。命令面
  板与快捷键帮助同步更新。
- **Alt+拖拽克隆**：按住 Alt 拖动实体 = 一步"复制并拖动"（Fusion/
  SolidWorks clone-drag）。复制在**首次移动时**才发生——普通 Alt+点击
  仍是边子选择，不留副本；拖动光标显示 copy，移动过的拖拽在释放时抑
  制尾随点击（避免误触边拾取）。E2E 实测：实体计数 1→2。

### 工程图 (Drawing)

- **详图视图 (Detail view, Fusion 式)**：工具栏 Detail 开关后点击任意
  投影视图，即以点击点为圆心裁剪 25mm 半径区域，2× 放大绘制在图纸
  下方自动增长的条带中——圆形边框 + "DETAIL A (2:1)" 标注（字母递
  增 A,B,…Z,AA）；源视图上以虚线圆标出裁剪区域。线段-圆弦裁剪与既有
  剖视半空间裁剪同族；右键删除面板。
- **可编辑文字注释 (Note)**：Note 开关后点击图纸任意处添加文字注释，
  内联输入框编辑（Enter 提交 / Esc 取消 / 空文本自动删除），悬停 ×
  或右键删除；注释随 SVG 导出为 `<text>`、随 PNG/PDF（画布快照路线）
  一并输出。
- 说明：工程图状态（含详图/注释）目前为会话态——`.studio3d` 尚未序
  列化任何工程图数据（剖切轴同样是组件局部态），未擅自扩展文件格式。

### AI

- **面拾取视觉确认**：AI 调 `select_face_at_viewport` 选中面时，除既有
  橙色高亮外弹出 toast 报出实体名——用户不用找高亮就知道 AI 点了谁。

## [0.10.0] — 2026-09-19

主题：**像桌面 CAD 一样顺手** — 拖放导入、约束可见、视图切换有动画、
特征范围不再靠猜。

### 新增 (Added)

- **拖放导入** — 把 STL / OBJ / STEP / 3MF 文件（或整个 `.studio3d` 项目）
  拖到窗口任意位置即可：全屏虚线浮层确认目标，网格文件合并进场景，
  项目文件经未保存确认后整体替换，不认识的格式弹出支持类型提示。
  菜单导入与拖放共用同一条导入路径（`lib/io/importFiles.ts`）。
- **草图约束符号**（Fusion/SolidWorks 式）— 已施加的约束在图元旁显示
  绿色徽标（H、V、∥、⊥、R、⊙、T 等，距离约束带数值如 D25.0），与琥珀
  色尺寸标签并排；草图规则从此一眼可见。
- **平滑视图过渡** — 标准视图切换（数字键 1-7、ViewCube、命令面板）
  由瞬移改为 250ms 球面插值动画（四元数 slerp，正视↔后视的 180° 翻转
  也平滑）；用户一旦拖动视角立即取消动画。
- **圆角/倒角范围显式化** — 右键特征菜单现在明确标注作用范围：“全部
  边圆角…”（无边选时）或“对 N 条选定边圆角…”（Alt+click 选边后），
  不再需要记住“空范围=全部”的隐含规则。

## [0.9.0] — 2026-09-19

闭环偏移补全 + 质量加固。

### 修复与补全 (Fixes & completion)

- **闭环偏移真正可用 (Loop offset works on hand-drawn profiles)** — 0.8.1 的
  工具栏 Offset 按钮对"手画的闭合轮廓"必然失败：环路检测只认共享 point
  id，而徒手画线工具每段都生成新点。现在接合点按**坐标**匹配（6 位小数
  键），手绘三角形/任意闭合折线都能偏移；同时修复了 CW 绕向环的正负号
  翻转错误（外法向已按绕向选取，无需再翻距离）。
- **自交拒绝** — 内向偏移超过内切圆半径时，尖角处的斜接点会跨到轮廓
  另一侧但不一定翻转鞋带面积符号（旧校验漏检）。新增标准的"新顶点必
  须落在原多边形正确一侧"逐点校验，越过即拒绝且零改动。
- **输出拓扑** — 偏移副本的接合点现在共享（三角形 3 点而非 6 点），
  与矩形工具生成的环同构，可继续链式偏移/成环拉伸。
- 新增 11 个闭环偏移测试（圆/弧/矩形/CCW/CW/塌缩拒绝/开链拒绝/斜接
  钳制上界），补上了此前为零覆盖的 `offsetSketchProfile` 路径。

## [0.8.1] — 2026-09-19

修复版 + 草图增强。

### 修复 (Fixes)

- **v0.8.0 桌面构建失败已修复**：BVH worker 客户端在无 Worker 环境下调用了
  未声明的 `computeBoundsTree()`，导致类型检查失败、四个平台的安装包
  全部构建失败。现在直接构造 `MeshBVH`，结果等价且类型干净。
  （v0.8.0 的 tag 因此没有可用的发布产物，请使用 0.8.1。）

### 新增 (Added)

- **草图等距偏移 (Offset entity)** — SolidWorks 式等距复制：选中线/圆/
  圆弧后，从草图工具栏或右键菜单输入带符号的偏移距离，线沿法向复制、
  圆/弧绕圆心缩放（负值向内，禁止缩到消失），新实体自动选中，单条
  草图撤销记录。

## [0.8.0] — 2026-09-19

Theme: **AI 面拾取闭环 + 视口性能清扫** — 补上 todo 中最后一个遗留项
（VLM 面拾取），并由并行审计清掉四个真实渲染热点。

### AI 与操作 (AI & interaction)

- **AI 面拾取闭环 (VLM face-picking)** — 此前 AI 只能"看"不能"点"：现在
  视口截图发给模型后，模型估计目标面上的坐标 (0..1)，调用新工具
  `select_face_at_viewport` → `scenelab:pick-face` 事件 → 视口用真实相机
  射线拾取并设置面选集（与 Ctrl+点击同路径），再链式调用 shell/fillet。
  裁剪区域截图的坐标自动映射回全视口；系统提示词指导"先拾取后修改"
  （面 id 每次重建后会变）。另加 `select_face`（按 id 直选）、
  `clear_face_selection`、`select_body` 三个选区控制工具。
- **数值表达式输入 (Fusion/SolidWorks 式)** — 所有数值对话框（共享
  NumericPrompt、拉伸、特征编辑）现在支持算术：输入 `20/2`、`(30-6)/3`、
  `2*pi*5` 提交计算值；非法输入红框提示且禁止提交；表达式实时预览结果
  （`= 10`）。手写递归下降解析器，零 eval/Function。
- **命令面板模糊搜索** — 从纯子串升级为市场标准模糊匹配：词边界加权、
  子序列匹配（`zmsl` → Zoom to selection）、多 token AND（`zoom sel`）、
  label > id > category 权重分层。
- **浏览树过滤框 (Fusion 式)** — 实体/参考几何/特征历史按名称实时过
  滤，显示 `n/total` 匹配计数，Esc 清空。
- **缩放命令入面板** — `view.fitAll` / `view.fitSelection` 注册进命令面
  板（F / Shift+F 原快捷键不变）。

### 性能 (Performance)

- **去掉 preserveDrawingBuffer** — 新的视口捕获服务在截图/AI 视觉/PNG
  导出前强制渲染一帧，省掉逐帧保留帧缓冲的 GPU 带宽开销。
- **相机事件门控** — `viewport-camera-update` 仅在相机真正移动时派发，
  悬停变色/草图预览不再触发 ViewCube 无效重渲染与逐帧事件分配。
- **草图预览零分配化** — 橡皮筋预览的材质（按尺寸缓存）与尺寸标签
  （LiveTextSprite 原地重绘单张 canvas）全部复用：鼠标移动不再创建
  canvas/CanvasTexture/GPU 上传。
- **resize 早退** — ResizeObserver 尺寸/DPR 未变时直接返回，不再重复
  重分配 drawing buffer。

## [0.7.0] — 2026-09-14

Theme: **对标主流 CAD 的深度打磨** — 补齐 Fusion 360 时间轴拖拽重排这一
标志性交互，并把视口两个真实性能热点做掉（拖拽逐帧重建、阴影逐帧重绘）。

### 操作 (Interaction)

- **时间轴拖拽重排 (Timeline drag-reorder)** — 底部时间轴的 Feature 芯片
  现在可以直接拖拽排序（Fusion 360 标志性交互）：拖拽时显示蓝色插入指
  示条，非法位置（跨越依赖关系）显示系统"禁止"光标并拒绝放置；合法移
  动即时重算。依赖规则：特征必须保持在其父特征之后、其消费者之前。
  键盘无障碍：聚焦芯片后 **Alt+←/→** 单步移动。一次拖拽 = 一条撤销记
  录；非法/原地放置不产生空撤销条目。
- **箭头微调步长修饰键** — ←↑→↓/PgUp/PgDn 微移所选：Shift = 10mm 粗步，
  **Alt = 0.1mm 细步**（新增），默认 1mm；草图内微移同步支持（乘网格
  步长）。与 Fusion 的修饰键分级一致。

### 性能 (Performance)

- **拖拽移动改预览变换 (Preview-transform drag)** — 之前每次 pointermove
  都重映射全部顶点/面/边并重建 mesh + 法线 + **BVH**，大模型拖拽必掉
  帧。现在拖拽期间只把偏移累积到 `dragOffset` 并施加为 `mesh.position`
  纯变换（零几何重建、零 GPU 重上传），松手时一次性烘焙成单条可撤销
  平移；Esc 取消只是丢弃预览（几何从未被碰过）。特征树实体照旧不动
  （设计如此），只有真正会移动的直接体参与预览。
- **阴影贴图按需重绘 (On-demand shadow map)** — `shadowMap.autoUpdate`
  关闭：只有几何增删/重建、可见性变化、拖拽预览移动或阴影开关时才重
  渲染阴影贴图，静止帧不再每帧重绘 2048² 深度图。
- **草图求解器性能护栏** — 新增基准测试：50 条约束的链式草图必须在
  100ms 内解完（规划文档中的指标终于有测试看守，防 O(n²) 回归）。

## [0.6.0] — 2026-09-04

Theme: **beginner-friendly, learned from the market leaders** — three deep UX
rounds that borrow the signature interactions of TinkerCAD, Fusion 360,
SolidWorks and Blender, plus the release hardening around them.

###建模与操作逻辑 (Modeling & interaction)

- **视口拖拽移动 (Drag bodies to move)** — 左键抓住实体在地面平面上滑动，
  网格自动吸附（Ctrl 自由移动），拖拽时实时显示 `Δ x, z mm` 位移读数；
  整次拖拽只占一条撤销记录，Esc 一键整体取消；抓取未选中的实体时会先
  选中它，整组一起移动。
- **草图尺寸点击编辑 (Click-to-edit sketch dimensions)** — 草图上的长度/
  半径标签现在可以直接点击，输入精确数值即可驱动图形：线段绕中点双向
  伸缩、圆/圆弧改半径且圆心不动。
- **实时剖切分析 (Live section analysis, X 键)** — 全局裁剪平面沿 X/Y/Z
  实时剖开视图：轴切换、贴合模型包围盒的偏移滑杆、翻转保留侧。纯视图
  检查，不改动几何。
- **原位粘贴 (Paste in place, Ctrl+Shift+V)** — 副本落在原始精确位置而非
  级联偏移，适合对位复制。
- **右键“最近使用”工具 (Recent tools in the right-click menu)** — 空白处
  右键菜单顶部显示最近执行过的命令，一键重跑（Fusion 式）。
- **缩放跟随光标 (Zoom-to-cursor)** — 滚轮缩放朝光标所指方向推进，而
  非屏幕中心（Fusion / Blender 行为）。
- **E 键拉伸、Ctrl+I 隔离** — 草图模式下按 E 直接打开拉伸；Ctrl+I 隔离
  所选实体。
- **撤销保留拖拽语义** — 拖拽/粘贴/微移统一为单步撤销；无位移的按下不
    产生空撤销记录。

### 新手体验 (Beginner experience)

- **快速零件库 (Parts library, B 键)** — 25 个参数化常用件的可搜索素材
  库：基础形状（实用尺寸）、机械件（M8 六角螺母、垫圈、轴套、法兰、
  L/U 型支架、z12 齿轮、旋钮）、M3–M8 孔铣刀、趣味件（五角星、金字
  塔、拱门、台阶）。点击插入并自动错位摆放；支持中英文搜索、分类筛
  选、最近使用；命令面板与 AI（`insert_library_part` /
  `load_sample_project` 工具）同源。
- **欢迎引导 (Welcome guide)** — 空场景时显示首启卡片：快捷动作（插入
  长方体 / 打开零件库 / 从草图开始 / 问 AI）、四个一键示例项目（手机
  支架、笔筒、齿轮组、铭牌）、跨会话持久化的 4 步新手任务清单（插入
  → 移动 → AI → 保存），由真实操作自动打勾。
- **Fusion 式底部时间轴** — 特征树以芯片形式按构建顺序展示（类型图标
  + 参数摘要）；单击选中产出实体、双击编辑、右键抑制/删除；空树自动
  隐藏。

### 视口与界面 (Viewport & UI)

- **地面阴影 (Ground shadows)** — 实体在网格下方投射柔和阴影（PCF 软
  阴影），视口观感对齐 Fusion/SolidWorks；状态栏一键开关并持久化。
- 状态栏提示扩展：子实体选择（Alt+边 / Ctrl+面）、拖拽移动、绘制时
  直接输入尺寸。
- 全部新功能提供中英双语文案与快捷键帮助条目。

### 质量与工程 (Quality)

- 单元测试 1695 → **1760**（122 个文件），新增覆盖：零件目录（体积/
  包围盒/贴合台面/双语命名）、示例项目、时间轴摘要与选中解析、剖切
  平面数学、拖拽撤销语义、零件库 store 动作、欢迎卡可见性规则、命令
  历史。
- E2E 冒烟套件 4/4：启动、右键插入、OCCT 曲面 STEP 导入、拖拽移动 +
  单步撤销回归。
- lint / tsc / vitest / build 全绿；版本号同步 package.json /
  Cargo.toml / tauri.conf.json。

## [0.5.0] — 2026-09-01

Deep-completion passes #1–#7: exact Manifold booleans + Web Worker offload,
parametric sweep/loft, feature-tree undo/redo, face-scoped shell, driving
drawing dimensions, type-ahead sketch dimensions, in-app numeric prompts,
drawing section views with PDF export, STEP/3MF round-trip, Tauri native
dialogs + autosave, Playwright E2E smoke suite. (Details in `todo.md`.)

## [0.4.0] — 2026-08-31

Comprehensive edge-case testing and QA pass across geometry, solver, booleans
and arrays.

## Earlier

See `todo.md` for the full engineering changelog.
