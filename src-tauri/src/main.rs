// SceneLab — Tauri 2 main entry point
// from /home/yfsama/vector/src-tauri/src/lib.rs (pattern reference)

#![cfg_attr(not(debug_assertions), windows_subsystem = "windows")]

fn main() {
    scenelab_lib::run()
}
